#!/usr/bin/env bash
# This script installs and starts Nginx so it returns HTTP 200 on port 80

# Update package list
apt-get update

# Install Nginx
apt-get install -y nginx

# Ensure necessary directories exist
mkdir -p /var/run /var/run/nginx /var/log/nginx

# Start Nginx as a daemon
nginx
