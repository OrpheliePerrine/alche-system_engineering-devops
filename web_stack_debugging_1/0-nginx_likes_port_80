#!/usr/bin/env bash
# This script installs and starts Nginx so it returns HTTP 200 on port 80

# Update packages and install Nginx
apt-get update
apt-get install -y nginx

# Fix missing directories (common in bare containers)
mkdir -p /var/run/nginx

# Start Nginx in the background
nginx
